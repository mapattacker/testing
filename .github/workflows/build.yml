name: build


on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]


jobs:

  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          sleep 30
          echo "hello"